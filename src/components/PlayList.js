import React from 'react';
import TrackList from './TrackList';

const PlayList = () => {
    return (
        <div>
            <h1>Playlist</h1>
            <TrackList />
        </div>
    );
};

export default PlayList;