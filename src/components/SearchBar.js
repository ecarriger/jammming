import React from 'react';

const SearchBar = () => {
    return (
        <form>
            <input type="text" />
            <input type="submit" value="Search" />
        </form>
    );
};

export default SearchBar;